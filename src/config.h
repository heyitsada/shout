#ifndef CONFIG_H
#define CONFIG_H

#include <stdbool.h>

#define DEFAULT_RATE 48000 /* This are Hz */
#define DEFAULT_BITS 16    /* 16 or 32 only */
#define DEFAULT_CHANNELS 2
#define BUFSIZE 65536

bool debug = false;

#endif
